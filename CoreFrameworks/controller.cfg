# Portfolio Controller Configuration
#
# warmup phase
warmup_ticks=64
#
# portfolio-level (controls buy gate regression adjustments)
poll_interval=100
r2_threshold=0.30
slope_scale_buy=0.50
max_shift=5.00
#
# per-position exit conditions (percentage-based)
take_profit_pct=3.00
stop_loss_pct=1.50
